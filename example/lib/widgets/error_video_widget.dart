import 'package:flutter/material.dart';

class ErrorVideoWidget extends StatelessWidget {
  const ErrorVideoWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Error'),
    );
  }
}
